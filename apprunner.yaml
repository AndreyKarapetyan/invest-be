version: 1.0
runtime: nodejs16
build:
  commands:
    pre-build:
      - node prebuild.js
    build:
      - npm i
    post-build:
      - npm run migrate:generate
run:
  runtime-version: 16.19.0
  command: npm start