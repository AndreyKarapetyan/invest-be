version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm i
      - node generate-env.js
    post-build:
      - npm run migrate:generate
run:
  runtime-version: 16.19.0
  command: npm start