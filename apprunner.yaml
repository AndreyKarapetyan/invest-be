version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm i
    post-build:
      - sh apprunner-env-download.sh
      - npm run migrate:generate
run:
  runtime-version: 16.19.0
  command: sh apprunner-env-download.sh && npm start