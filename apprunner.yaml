version: 1.0
runtime: nodejs16
build:
  commands:
    build:
      - npm i
    post-build:
      - curl -o .env https://investinfrastructurestack-envbucket236fc6cf-1vp4shezew7lg.s3.eu-west-1.amazonaws.com/.env
      - npm run migrate:generate
run:
  runtime-version: 16.19.0
  command: npm start